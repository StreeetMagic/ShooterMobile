using UnityEngine;

namespace Gameplay.Characters.Enemies.Spawners.SpawnPoints
{
    public class SpawnPoint : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
